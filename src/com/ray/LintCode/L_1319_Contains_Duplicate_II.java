package com.ray.LintCode;

import com.ray.io.Out;

/**
 * Description :
 *   Given an `array` of integers and an integer `k`, find out whether there are two distinct indices `i` and `j` in the array such that `nums[i] = nums[j]` and the **absolute** difference between `i` and `j` is at most `k`.
 *
 * Example :
 *   **Example 1：**
```
Input：nums = [1,2,1], k = 0
Output：False
```

**Example 2：**
```
Input：nums = [1,2,1], k = 2
Output：True
Explanation：nums[0] = nums[2] and 2 - 0 <= 2
```
 *
 * Challenge :
 *   
 *
 * @author rays1
 * @url    https://www.lintcode.cn/problem/contains-duplicate-ii/description
 * @date   2019-07-03 17:44:29
 */
public class L_1319_Contains_Duplicate_II {

    static class Solution {
    
        
    
    }
    
    public static void main(String[] args) {
        
        Out.p(new Solution());
        
    }

}
