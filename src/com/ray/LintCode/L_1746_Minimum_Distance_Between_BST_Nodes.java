package com.ray.LintCode;

import com.ray.io.Out;

/**
 * Description :
 *   Given a Binary Search Tree (BST) with the root node root, return the minimum difference between the values of any two different nodes in the tree.
 *
 * Example :
 *   **Example 1:**
 *   ```
 *   Input: root = {2,1}
 *   Output: 1
 *   Explanation:
 *   Note that root is a TreeNode object, not an array.
 *   
 *   The given tree {2,1} is represented by the following diagram:
 *   
 *         2
 *        / 
 *       1
 *   
 *   while the minimum difference in this tree is 1, it occurs between node 1 and node 2, also between node 3 and node 2.
 *   ```
 *   **Example 2:**
 *   ```
 *   Input: root = {4,2,6,1,3}
 *   Output: 1
 *   Explanation:
 *   Note that root is a TreeNode object, not an array.
 *   
 *   The given tree [4,2,6,1,3,null,null] is represented by the following diagram:
 *   
 *             4
 *           /   \
 *         2      6
 *        / \    
 *       1   3  
 *   
 *   while the minimum difference in this tree is 1.
 *   ```
 *
 * Challenge :
 *   
 *
 * @author rays1
 * @url    https://www.lintcode.cn/problem/minimum-distance-between-bst-nodes/description
 * @date   2019-07-03 17:45:43
 */
public class L_1746_Minimum_Distance_Between_BST_Nodes {

    static class Solution {
    
        
    
    }
    
    public static void main(String[] args) {
        
        Out.p(new Solution());
        
    }

}
