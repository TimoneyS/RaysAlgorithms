package com.ray.LintCode;

import com.ray.io.Out;

/**
 * Description :
 *   Given two strings `A` and `B` of lowercase letters, return true if and only if we can swap two letters in `A` so that the result equals `B`.
 *
 * Example :
 *   **Example 1:**
 *   ```
 *   Input: A = "ab", B = "ba"
 *   Output: true
 *   ```
 *   **Example 2:**
 *   ```
 *   Input: A = "ab", B = "ab"
 *   Output: false
 *   ```
 *   **Example 3:**
 *   ```
 *   Input: A = "aa", B = "aa"
 *   Output: true
 *   ```
 *   **Example 4:**
 *   ```
 *   Input: A = "aaaaaaabc", B = "aaaaaaacb"
 *   Output: true
 *   ```
 *   **Example 5:**
 *   ```
 *   Input: A = "", B = "aa"
 *   Output: false
 *   ```
 *
 * Challenge :
 *   
 *
 * @author rays1
 * @url    https://www.lintcode.cn/problem/buddy-strings/description
 * @date   2019-07-03 17:45:38
 */
public class L_1510_Buddy_Strings {

    static class Solution {
    
        
    
    }
    
    public static void main(String[] args) {
        
        Out.p(new Solution());
        
    }

}
