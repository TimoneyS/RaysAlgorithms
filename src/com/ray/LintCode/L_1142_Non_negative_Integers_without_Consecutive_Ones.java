package com.ray.LintCode;

import com.ray.io.Out;

/**
 * Description :
 *   Given a positive integer n, find the number of **non-negative** integers less than or equal to n, whose binary representations do NOT contain **consecutive ones**.
 *
 * Example :
 *   **Example 1:**
 *   ```
 *   Input: 5
 *   Output: 5
 *   Explanation: 
 *   Here are the non-negative integers <= 5 with their corresponding binary representations:
 *   0 : 0
 *   1 : 1
 *   2 : 10
 *   3 : 11
 *   4 : 100
 *   5 : 101
 *   Among them, only integer 3 disobeys the rule (two consecutive ones) and the other 5 satisfy the rule. 
 *   ```
 *   
 *   
 *   **Example 2:**
 *   ```
 *   Input：6
 *   Output：5
 *   Explanation：
 *   Here are the non-negative integers <= 6 with their corresponding binary representations:
 *   0 : 0
 *   1 : 1
 *   2 : 10
 *   3 : 11
 *   4 : 100
 *   5 : 101
 *   6 : 110
 *   Among them,  integer 3 and 6 disobey the rule (two consecutive ones) and the other 5 satisfy the rule. 
 *   ```
 *
 * Challenge :
 *   
 *
 * @author rays1
 * @url    https://www.lintcode.cn/problem/non-negative-integers-without-consecutive-ones/description
 * @date   2019-07-03 17:39:37
 */
public class L_1142_Non_negative_Integers_without_Consecutive_Ones {

    static class Solution {
    
        
    
    }
    
    public static void main(String[] args) {
        
        Out.p(new Solution());
        
    }

}
