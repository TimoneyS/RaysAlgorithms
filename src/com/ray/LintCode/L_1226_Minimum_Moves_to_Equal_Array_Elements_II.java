package com.ray.LintCode;

import com.ray.io.Out;

/**
 * Description :
 *   Given a **non-empty** integer array, find the minimum number of moves required to make all array elements equal, where a move is incrementing a selected element by 1 or decrementing a selected element by 1.
 *   
 *   You may assume the array's length is at most 10,000.
 *
 * Example :
 *   Example 1:
 *   ```
 *   Input:
 *   [1,2,3]
 *   
 *   Output:
 *   2
 *   
 *   Explanation:
 *   Only two moves are needed (remember each move increments or decrements one element):
 *   
 *   [1,2,3]  =>  [2,2,3]  =>  [2,2,2]
 *   ```
 *   
 *   Example 2:
 *   ```
 *   Input:
 *   [1,2]
 *   
 *   Output:
 *   1
 *   
 *   Explanation:
 *   Only one move is needed (remember each move increments or decrements one element):
 *   
 *   [1,2]  =>  [2,2]
 *   ```
 *
 * Challenge :
 *   
 *
 * @author rays1
 * @url    https://www.lintcode.cn/problem/minimum-moves-to-equal-array-elements-ii/description
 * @date   2019-07-03 17:41:37
 */
public class L_1226_Minimum_Moves_to_Equal_Array_Elements_II {

    static class Solution {
    
        
    
    }
    
    public static void main(String[] args) {
        
        Out.p(new Solution());
        
    }

}
