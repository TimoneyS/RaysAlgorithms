package com.ray.LintCode;

import com.ray.io.Out;

/**
 * Description :
 *   Given an array `A` of positive integers, and two positive integers `L` and `R` `(L <= R)`.
 *   
 *   Return the number of (contiguous, non-empty) subarrays such that the value of the maximum array element in that subarray is at least `L` and at most `R`.
 *
 * Example :
 *   **Example 1:**
 *   
 *   ```
 *   Input: A = [2, 1, 4, 3], L = 2, R = 3
 *   Output: 3
 *   Explanation: There are three subarrays that meet the requirements: [2], [2, 1], [3].
 *   ```
 *   
 *   **Example 2:**
 *   
 *   ```
 *   Input: A = [7,3,6,7,1], L = 1, R = 4
 *   Output: 2
 *   ```
 *
 * Challenge :
 *   
 *
 * @author rays1
 * @url    https://www.lintcode.cn/problem/number-of-subarrays-with-bounded-maximum/description
 * @date   2019-07-03 17:36:37
 */
public class L_1021_Number_of_Subarrays_with_Bounded_Maximum {

    static class Solution {
    
        
    
    }
    
    public static void main(String[] args) {
        
        Out.p(new Solution());
        
    }

}
